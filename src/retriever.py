"""
Retriever module (future use).
Right now it only returns None.
Later, you can replace with FAISS or Chroma retriever.
"""

def get_retriever():
    # 🔹 Placeholder retriever (not used in GPT-only chatbot)
    return None

